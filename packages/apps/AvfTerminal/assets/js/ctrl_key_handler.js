// Ctrl key handler for terminal - placeholder
// This script maps Ctrl key combinations in the terminal WebView
(function() {
    if (typeof window._ctrlKeyHandlerInstalled === 'undefined') {
        window._ctrlKeyHandlerInstalled = true;
    }
})();
